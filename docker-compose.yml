services:
  rtmp:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${HTTP_BIND:-127.0.0.1}:${HTTP_PORT:-8080}:80"
      - "${RTMP_PORT:-1935}:1935"
    volumes:
      - rec-data:/tmp/rec
      - key-data:/data

volumes:
  rec-data:
  key-data:
